import SwiftUI

enum GridColumns {
    static let cocktails = [GridItem(.adaptive(minimum: 160), spacing: 12)]
    static let ingredients = [GridItem(.adaptive(minimum: 80), spacing: 10)]
    static let libraries = [GridItem(.adaptive(minimum: 160), spacing: 16)]
}
